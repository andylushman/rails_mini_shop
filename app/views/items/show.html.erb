<p id="notice"><%= notice %></p>
<div class=<%= @item.active ? "active" : "inactive" %>>
  <h1><%= @item.name %></h1>
  <p>Description: <%= @item.description %></p>
  <p>Price: $<%= @item.price %></p>
  <p>Image (Pending): <%= @item.image %></p>
  <p>Quantity Remaining: <%= @item.inventory %></p>
  <p>Merchant: <%= link_to @item.merchant.name, merchant_path(@item.merchant) %></p>
</div>


<%= link_to 'Edit', edit_merchant_item_path(@item.merchant, @item) %>
<%= link_to 'Back', items_path %>



